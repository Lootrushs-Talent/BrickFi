const {
  requirePropertyId,
  requireAddressParam,
  requireJsonBody,
  requireCartItems,
} = require("../routes/error");

module.exports = {
  requirePropertyId,
  requireAddressParam,
  requireJsonBody,
  requireCartItems,
};
